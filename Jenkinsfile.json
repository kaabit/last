node {
  stage ('preparation') {
   git 'https://github.com/kaabit/last'
  }
  stage ('buld') {
   echo 'building in process' 
  }
  stage ('test and deployement') {
   echo 'testing in process'
  }
}
