pipeline {
  stage ('preparation') {
   git 'https://github.com/kaabit/last'
  }
  stage ('buld') {
   sh echo 'building in process' 
  }
  stage ('test and deployement') {
    sh echo 'testing in process'
  }
}
